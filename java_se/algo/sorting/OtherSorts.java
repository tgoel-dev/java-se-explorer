package algo.sorting;

public class OtherSorts {

	/*
	 * Mathematically, sorting via comparison cannot go lower than O(N*log(N)), but
	 * sorting without comparison can improve the time.
	 * 
	 * Non-comparison sorts: extremely complicated algos that use binary data
	 * addressing (analog storage). 
	 * 
	 * 1. Counting Sort 
	 * 2. Radix Sort
	 *
	 * Time Complexity = O(N+K), slightly better than O(N*log(N)).
	 * 
	 * (--) Limited use case.
	 */
}
